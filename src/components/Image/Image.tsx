import React from 'react';
import {ImageProps} from 'react-native-ui-lib';
import BaseImage from 'src/components/base/BaseImage';
const Image = (props: ImageProps) => {
  return <BaseImage {...props} />;
};

export default Image;
